// Como podemos rodar isso em um arquivo .ts sem causar erros? 

//let employee = {};
//employee.code = 10;
//employee.name = "John";

interface Empregado {
    code: number,
    nome: string
}

let empregado1: Empregado = {
    code: 10,
    nome: 'João'
}
